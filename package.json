{
  "name": "trustnotes-guardian",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4319 --strictPort",
    "spike": "vite build --config vite.spike.config.ts && node run-spike.mjs",
    "e2e": "vite build && node e2e.mjs",
    "typecheck": "tsc --noEmit"
  }
}
